import { StatusBar } from 'expo-status-bar';
import { StyleSheet, Text, View } from 'react-native';

function nykon() {
  return(
    <View>
      <Text style={{fontSize: 40 + 'px', color: 'purple', textAlign: 'center'}}>Хуй</Text>
      <StatusBar style="auto" />
    </View>
  );
}

export default nykon();